<p>
  <b>Comp:</b>
  <%= @comp.name %>
</p>


<table>
  <tr>
    <th>Пользователи</th>
  </tr>

<% @comp.sortuserlog.each do |u| %>
  <tr>
    <td><%= link_to u.user_name, u.user %></td>
    <td><%= u.in %></td>
    <td><%= u.created_at.to_formatted_s(:short) %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Back', user_path %>
