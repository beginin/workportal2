<h1>Listing userlogs</h1>

<table>
  <tr>
    <th>User</th>
    <th>Last comp</th>

  </tr>

<% @user.each do |u| %>
  <tr>
    <td><%= link_to u.name, userid_path(u) %></td>
    <td><%= link_to u.sortuserlog.last.comp_name,u.sortuserlog.last.comp %></td>


  </tr>
<% end %>
</table>





<br />

