<p>
  <b>User:</b>
  <%= @user.name %>
</p>


<table>
  <tr>
    <th>Компьютер</th>
  </tr>

<% @user.sortuserlog.each do |u| %>
  <tr>
    <td><%= link_to u.comp_name, u.comp %></td>
    <td><%= u.in %></td>
    <td><%= u.created_at.to_formatted_s(:short) %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Back', user_path %>